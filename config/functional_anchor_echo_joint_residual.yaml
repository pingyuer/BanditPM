defaults:
  - functional_anchor_echo
  - _self_

exp_id: "functional_anchor_echo_joint_residual"

model:
  functional_anchor:
    training_stage: "joint_residual"
    prediction_mode: "anchor_primary"
