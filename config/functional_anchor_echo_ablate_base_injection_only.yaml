defaults:
  - functional_anchor_echo_joint_residual
  - _self_

exp_id: "functional_anchor_echo_ablate_base_injection_only"
ablation_type: "base_injection_only"

model:
  functional_anchor:
    use_anchor_features_in_residual: false
    anchor_logit_temperature:
      enabled: true
      init: 0.1
      learnable: false
      min: 0.1
      max: 2.0
    lambda_anchor: 0.0
    lambda_phase_consistency: 0.0
    lambda_anchor_temporal: 0.0
    lambda_slot_area_order: 0.0
    lambda_phase_slot_correlation: 0.0
