defaults:
  - functional_anchor_echo
  - _self_

exp_id: "functional_anchor_ablate_no_residual_constraint"

model:
  functional_anchor:
    lambda_residual_smallness: 0.0
    lambda_boundary_residual: 0.0
