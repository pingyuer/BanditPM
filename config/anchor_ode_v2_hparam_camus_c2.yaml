defaults:
  - anchor_ode_v2_tuned_camus
  - _self_

exp_id: "anchor_ode_v2_hparam_camus_c2"


model:
  anchor_ode:
    guidance_proj_hidden_dim: 16
    guidance_fusion_mode: "residual_add"

main_training:
  unext_lr_ratio: 1.0
  anchor_ode_lr_ratio: 1.0

augmentation:
  enabled: false

evaluation:
  protocol_version: "v3_current_anchor_affine"
  postprocess:
    enabled: true
    min_size: 16
