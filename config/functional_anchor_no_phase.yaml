defaults:
  - functional_anchor_echo
  - _self_

exp_id: "functional_anchor_no_phase"

model:
  functional_anchor:
    phase:
      source_priority: ["normalized_time"]
      normalized_time_weight: 0.0
    lambda_phase_consistency: 0.0
    lambda_phase_slot_correlation: 0.0

