defaults:
  - /geomaskformer_echo
  - _self_

exp_id: "geomaskformer_k40_echo"

model:
  geomaskformer:
    num_queries: 40
    loss:
      topk: 4

mlflow:
  tags:
    search_axis: "num_queries"
    num_queries: "40"
