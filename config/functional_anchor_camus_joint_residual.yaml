defaults:
  - functional_anchor_camus
  - _self_

exp_id: "functional_anchor_camus_joint_residual"

model:
  functional_anchor:
    training_stage: "joint_residual"
    prediction_mode: "anchor_primary"
