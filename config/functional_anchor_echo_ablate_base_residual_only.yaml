defaults:
  - functional_anchor_echo_joint_residual
  - _self_

exp_id: "functional_anchor_echo_ablate_base_residual_only"
ablation_type: "base_residual_only"

model:
  functional_anchor:
    disable_injector: true
    lambda_anchor: 0.0
    lambda_phase_consistency: 0.0
    lambda_anchor_temporal: 0.0
    lambda_slot_area_order: 0.0
    lambda_phase_slot_correlation: 0.0
    temporal_state:
      detach_state: true
      detach_every: 1
      update_scale: 0.0
