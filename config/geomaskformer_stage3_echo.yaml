defaults:
  - geomaskformer_echo
  - _self_

exp_id: "geomaskformer_stage3_echo"

model:
  geomaskformer:
    training_stage: "stage3"
    protocol:
      no_mask: 0.0
      first_frame: 1.0
      random_keyframe: 0.0
    loss:
      visible_reconstruction: 0.05

mlflow:
  tags:
    protocol: "stage3_ed_es_visible_completion"
