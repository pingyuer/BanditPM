defaults:
  - config_dynakey_baseline
  - _self_

exp_id: "dynakey_v2_noleak"

phase_init:
  train: "pred_or_zero"
  val: "pred_or_zero"
  test: "pred_or_zero"

evaluation:
  init_mode: "pred_or_zero"
  exclude_init_frame: true
  init_frame_index: 0
  protocol_version: "v2_no_leak"

wandb:
  group: "dynakey_v2_no_leak"
  tags: ["dynakey", "kpff", "ode_memory", "v2_no_leak", "predinit", "exclude_init_frame"]
