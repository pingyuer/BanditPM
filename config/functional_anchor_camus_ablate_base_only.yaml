defaults:
  - functional_anchor_camus_joint_residual
  - _self_

exp_id: "functional_anchor_camus_ablate_base_only"
ablation_type: "base_only"

model:
  functional_anchor:
    disable_injector: true
    trust_max: 0.0
    residual_scale:
      enabled: true
      init: 0.0
      max: 0.0
      warmup_iters: 1
    lambda_anchor: 0.0
    lambda_residual_smallness: 0.0
    lambda_boundary_residual: 0.0
    lambda_phase_consistency: 0.0
    lambda_anchor_temporal: 0.0
    lambda_slot_area_order: 0.0
    lambda_phase_slot_correlation: 0.0
    lambda_ode_raw_delta: 0.0
