defaults:
  - anchor_ode_v2_tuned_camus
  - _self_

exp_id: "anchor_ode_v2_hparam_camus_c3"


model:
  anchor_ode:
    decoder_guidance_enabled: false

main_training:
  unext_lr_ratio: 1.0
  anchor_ode_lr_ratio: 1.0

augmentation:
  enabled: false

evaluation:
  protocol_version: "v3_current_anchor_affine"
  postprocess:
    enabled: true
    min_size: 16
